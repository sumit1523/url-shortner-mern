{
  "scripts": {
    "dev": "concurrently \"npm run start --prefix server-app\" \"npm run dev --prefix client-app\"",
    "build": "npm run build --prefix client-app && npm run start --prefix server-app",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5"
  }
}
